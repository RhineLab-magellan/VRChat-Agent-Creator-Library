---
title: "UdonSharpAgent 知识库发布就绪检查（2026-08-20）"
type: review
category: meta
knowledge_level: governance
status: active
source_type: local
version: 1.0
created: 2026-08-20
updated: 2026-08-20
tags:
  - review
  - audit
  - publish
  - readiness
---

# 发布就绪检查报告：UdonSharpAgent LLMWikiPage（2026-08-20）

> 检查对象：`C:\Users\59559\Desktop\Agent\UdonSharpAgent\LLMWikiExample\LLMWikiPage\`（知识页全树）
> 发布规模口径：**414 个 md 文件 / 非 md 0**（entities/concepts/comparisons/queries/meta + 顶层 `FACT.md`/`index.md`/`_always-load.md`）
> 检查依据：`LLMWiki/SCHEMA.md`（frontmatter / 标签分类法 §4 / 置信度 §9 / 链接规范 §7）
> 检查方法：`辅助文件/脚本/auxiliary-script/publish-readiness-check.py`（只读、幂等，二次运行 SHA-256 一致 = `9C81…303C` 版前的修正版，最终版幂等已验证）+ `site-scan.py --json`（只读）+ `factcheck-audit.py`（只读）+ 主代理独立 `read` 抽查。
> 交叉口径：site-scan 知识域（不含 meta）= 363 页｜factcheck-audit（含 meta，不含顶层治理）= 411 页｜本检查全量 = 414 页。

---

## 〇、总体结论

**尚未完全就绪**。四条发布要求中：

| # | 发布要求 | 判定 | 摘要 |
|---|---|---|---|
| 1 | 知识库内页完全自包含（仅 URL 以外无路径依赖） | 🔴 **未通过** | wikilink 死链 0（强项），但 13 页死链相对文件链接 + 22 页 `scheme 缺失`的站点相对 URL，发布后即坏 |
| 2 | 内容置信度及格 | 🟡 **基本及格** | 414 页中 1 页 confidence 值非规范（`Medium-High`）；low 2 页均带来源+理由 |
| 3 | 文档描述文件 Fact Index 正确 | 🔴 **不正确** | `FACT.md` 知识库结构树仍为旧 `memory/` 拓扑；3 个 shader index 存在 5 处死链 md 链接；58 页孤页未获索引 |
| 4 | 文档 Tag 正确 | 🟡 **基本合规，需收敛** | tags 缺失 0；分类法外 256 种/287 次（233 种单次按规范可保留，23 种 ≥2 次复用候选需注册） |

**发布前必须处理的硬阻塞**：C1 的 13 页死链 + 22 页站点相对 URL；C3 的 FACT.md 结构树更新 + 58 孤页补索引。
**需用户决策**：C1 的 sources→raw 本地锚点（4 页）与绝对路径示例（10 页）去留；C2 的 `unity-mcp.md` confidence 值修正方式。
**打包动作**：已按「检查完成后打包」要求生成快照 zip（含本报告 + 发布说明），见本报告末尾。

---

## 一、C1 自包含

> 口径：库内页只依赖可解析的 `[[wikilink]]` 与 `http(s)://` URL；其余本地路径均视为依赖项分类上报。

### 1.1 强项（通过项）

- **wikilink 真死链 = 0**（自研脚本按 basename/标题/别名身份解析 414 页，与 site-scan `dead_links=0` 交叉一致）。
- `[[[` 畸形括号 = 0；`LLMWikiExample/` 路径文本 = 0。
- 正文 `memory/` 引用收敛：

| 页 | 行号 | 性质 |
|---|---|---|
| `FACT.md` | :53 / :291 | 知识库结构树根 + 治理规则仍写 `memory/`（见 C3，应为旧拓扑遗留） |
| `meta/obsidian-governance.md` | :33/42/204/224 | 治理层对旧 Vault Root 的描述（描述性） |
| `comparisons/filamented-comparison.md` | :158 | tree 示意（描述性） |

> 注：`entities/avatar/optimization-guide.md:289` 的 `memory/filesize` 经核实为英文单词（内存/文件体积），非路径引用，已剔除。

### 1.2 硬违例 A — 死链相对文件链接（13 页 / 19 处）

| 页 | 失效链接 |
|---|---|
| `comparisons/orl-comparison.md` | `./overview.md` `./shader-list.md` `./configurable.md` `../liltoon/index.md` `../scss.md`（实际位于 `entities/avatar/shader/orl/`） |
| `entities/avatar/shader/filamented/index.md` | `comparison.md`（不存在） |
| `entities/avatar/shader/orl/index.md`｜`configurable.md`｜`overview.md`｜`shader-list.md` | `./comparison.md`（不存在） |
| `entities/avatar/shader/poiyomi/index.md` | `./pro-vs-toon.md`（L69/104/174）、`./comparison-liltoon.md`（L70/238）——正确目标在 `comparisons/poiyomi-pro-vs-toon.md`、`comparisons/poiyomi-vs-liltoon.md` |
| `entities/avatar/physbones.md` | `../../world/udon/networking/events.md`（应为 `../../world/udon/networking/events` 的正确层级） |
| `entities/world/scene-components/unity-constraints.md` | `../avatar/physbones.md`（应为 `../../avatar/physbones.md`） |
| `entities/world/scene-components/vrc-objectsync.md` | `../../patterns/manual-sync-state.md`、`../../patterns/owner-authoritative-interaction.md`（实际在 `concepts/patterns/`） |
| `entities/world/scene-components/vrc-scenedescriptor.md` | `../../patterns/`（指向不存在的目录） |
| `entities/world/udon/udonsharp/attributes.md` | `../../../FACT.md#…`（层级过深，应 `../../../../FACT.md`） |
| `entities/avatar/avatar-size-limits.md` | `../../参考文献\18-vrchat-CI4-notes\00-SUMMARY.md`（指向包外的 `memory/raw/参考文献`，打包后必断） |

> 说明：`entities/world/udon/persistence/serialization.md:186` 的 `byte[](4 字节/个,小端序)` 经核实为 **C# 代码注释误报**，已从死链剔除。

### 1.3 硬违例 B — 站点相对 URL（22 页，缺 scheme+host）

以下链接以 `/worlds/…`、`/docs/…`、`/platforms/…` 起始，缺少 `https://docs.vrchat.com` 前缀，发布后对外不可用（属「相对 URL 未补全主机」而非文件路径）：

`teaching-methodology.md`、`examples/ai-navigation.md`、`examples/detect-controller-collide.md`、`examples/image-loading.md`、`examples/midi-playback.md`、`examples/minimap.md`、`examples/mute-others.md`、`examples/persistence/unlock-items.md`、`examples/player-join-zones.md`、`examples/screen-canvas.md`、`examples/udon.md`、`sdk-prefabs.md`、`udon/ai-navigation.md`、`udon/animation-events.md`、`udon/avatar-events.md`、`udon/debugging-udon-projects.md`、`udon/event-execution-order.md`、`udon/external-urls.md`、`udon/image-loading.md`、`udon/input-events.md`、`udon/string-loading.md`、`whitelisted-world-components.md`

（典型：`/worlds/udon/ai-navigation`、`/worlds/udon/midi#midinoteon`、`/platforms/android/quest-content-limitations#components`……共约 47 处。）

### 1.4 软项 / 需用户决策

- **绝对路径 10 页**（均为正文/命令示例，非跨文件依赖）：`concepts/hybrid/vcc.md`、`concepts/rules/udonsharp-deep-pitfalls.md`、`entities/avatar/avatar-dynamic-bone-limits.md`、`entities/avatar/avatar-particle-system-limits.md`、`entities/platform/backup-strategy.md`、`entities/platform/unity-admin-warning.md`、`entities/world/udon/debugging-udon-projects.md`、`entities/world/udon/networking/debugging.md`、`entities/world/udon/using-build-test.md`、`meta/obsidian-governance.md`。例：`using-build-test.md:88-90` 的 `C:\Program Files (x86)\Steam\...\VRChat.exe`。
- **sources→raw 本地锚点 4 页**（`sources: [../../memory/raw/...]`，与「排除 raw 打包」冲突）：`entities/api/animator.md`、`entities/api/exposed-types.md`、`entities/api/not-exposed.md`、`entities/api/udon-type-exposure.md`。

**C1 判定：未通过**（需修 1.2 + 1.3；1.4 决策后定稿）。

---

## 二、C2 置信度

- **有效值分布（414 页）**：high 204（49.3%）／medium 207（50.0%）／low 2（0.5%）。
- **违规 1 项**：`entities/avatar/unity-mcp.md` frontmatter `confidence: Medium-High`（2026-08-19 新增页）——非 SCHEMA §9.4 枚举值 `high/medium/low`，应改为 `medium`（该页正文自述「部分工具级细节为推断」）。
- **low 2 页复核（均带来源+理由，符合 §9.4「须复核」）**：
  - `entities/avatar/commission-pricing.md`：`confidence: Low` + 正文 `⚠️ [INFERENCE] 价格区间为社区经验,无统一标准` + `sources: https://docs.vrcd.org.cn/books/86vrchat`。
  - `entities/platform/meta-quest-troubleshooting.md`：`confidence: Low` + 声明「候选专题 / 低到中置信度定位」 + `sources: docs.vrcd.org.cn/books/meta-quest + 分类审查归纳`。
- **改进建议（非本判定的阻塞项）**：factcheck-audit 口径 `sources` 结构化锚定率 25.1%（103/411），锚定缺口 9 页（SCHEMA §9.6 目标 ≥95%）——属「可信度治理层」短板，已在 2026-08-18 审查指出。

**C2 判定：基本及格**（1 项小违规 `unity-mcp.md`；其余已达标）。

---

## 三、C3 Fact Index（FACT.md + 各域 index.md）

- **`FACT.md` 结构树过时**：第 50-69 行「知识库结构」仍为旧 `memory/` 拓扑（root=`memory/`，子目录 `avatar/ shader/ world/ …`），而实际顶层为 `comparisons concepts entities meta queries` + 顶层 3 文件；:291「所有知识库索引必须指向 memory/ 目录内部」规则与 2026-08-10 迁移后的 `LLMWikiExample/LLMWikiPage/` 物理布局不一致 → **FACT.md 未随迁移更新**。
- **各域 index.md**：
  - 重复索引页（任意 `index.md` 的 `[[wikilink]]`）**死链 = 0**（强项）。
  - 但 **5 处 / 7 条 md 相对链接死链**集中在 shader 域：
    - `entities/avatar/shader/filamented/index.md` → `comparison.md`
    - `entities/avatar/shader/orl/index.md` → `./comparison.md`
    - `entities/avatar/shader/poiyomi/index.md` → `./pro-vs-toon.md`(×3)、`./comparison-liltoon.md`(×2)
- **孤页 58（无任何入链，排除 meta 与顶层治理页）**；site-scan 口径（知识域）46。主要分布：`entities/world/scene-components/*`（约 20 页 2026-08 新增专页）、`entities/world/examples/*`（obstacle-course/persistence/udon-example-scene 子页面）、`concepts/rules/*`（multi-vm-rules、udon-vm-architecture、udonsharp-deep-pitfalls、vrchat-api-exposure）、`entities/platform/meta-quest-troubleshooting.md` 等 → 索引未覆盖新页，读者从索引无法抵达。

**C3 判定：不正确**（FACT.md 需按新拓扑重写结构树；shader index 补链/改链；58 孤页入索引）。

---

## 四、C4 标签

- **tags 缺失 = 0**（414/414 页均有 tags，强项）。
- **分类法外标签 256 种 / 287 处**（SCHEMA §4 现登记 127 标签）：
  - **233 种为单次使用**（按 §4「1 次 → 页面特定标签，允许保留但不再注册」→ 合规可保留）。
  - **23 种 ≥2 次复用候选（共约 54 处），按 §4「2~4 次有复用价值才注册」应评估后登记**：`bakery`(4)、`portal`(4)、`player`(4)、`gesture`(3)、`workflow`(3)、`config`(2)、`wardrobe`(2)、`ux`(2)、`unity-native`(2)、`trust`(2)、`tools`(2)、`station`(2)、`sdk-3-10`(2)、`scene-components`(2)、`safety`(2)、`whitelist`(2)、`accessories`(2)、`patreon`(2)、`moderation`(2)、`integral`(2)、`bone-binding`(2)、`compression`(2)、`2026`(2)。
- 与 site-scan 口径（分类法外 248 种）基本一致，差异源于解析口径（site-scan 未含顶层治理页标签；本检查含）。

**C4 判定：基本合规**（0 缺失；23 种复用标签待登记收敛，非硬阻塞）。

---

## 五、打包交付

已创建发布快照（按「检查完成后打包」要求，无论是否就绪均打包并将结论随包交付）：

- **压缩包**：`C:\Users\59559\Desktop\Agent\UdonSharpAgent\UdonSharpAgent-知识库发布检查-2026-08-20.zip`
- 包结构（UTF-8 条目名）：
  - `UdonSharpAgent-LLMWikiPage/LLMWikiExample/LLMWikiPage/**`（414 个 md，相对路径原样）
  - `UdonSharpAgent-LLMWikiPage/发布检查-UdonSharpAgent-2026-08-20.md`（本报告副本）
  - `UdonSharpAgent-LLMWikiPage/README-发布说明.md`
- 工作区产物（不入包）：检查脚本 `辅助文件/脚本/auxiliary-script/publish-readiness-check.py`、原始 JSON `辅助文件/报告/publish-readiness-2026-08-20.json`、脚本摘要 `辅助文件/报告/publish-readiness-2026-08-20-脚本摘要.md`。

---

## 六、局限与说明

1. **检查口径为「全量 414 页」**（含 meta 与顶层治理），与 site-scan(363)/factcheck(411) 不同是预期差异，已在报告各节注明，不构成数据矛盾。
2. **wikilink 解析采用「身份匹配」**（basename/标题/别名），较 site-scan 的严格路径解析略宽松；两套口径下死链均为 0，结论一致。
3. **站点相对 URL 判定为「缺主机」**：`/worlds/…` 类链接在 VRChat 文档站内有效，但脱离了 docs.vrchat.com 主机后即为失效 URL，故计为发布硬违例。
4. **未做任何自动修复**：本报告所有发现均为只读审计结果；修复立项、标签注册、FACT.md 更新须在你确认后另行执行。
5. **检查脚本幂等已验证**：二次运行输出 SHA-256 一致（`IDEMPOTENT=True`）。
